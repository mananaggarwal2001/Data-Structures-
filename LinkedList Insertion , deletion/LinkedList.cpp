#include<bits/stdc++.h>
using namespace std;
int main()
{
    int count=0;
    for(int i=0;i<4;i++){
        for (int j = 0; j < 4; j++)
        {
            count++;
        }

    }
    cout<<"The value of the count is: "<<count<<endl;
    return 0;
}